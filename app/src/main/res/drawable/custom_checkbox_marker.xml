<!-- res/drawable/checkbox_marker.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado quando o checkbox está selecionado -->
    <item android:state_checked="true">
        <!-- Definindo o sinal de visto (marcador) com a cor desejada -->
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" /> <!-- Cor do fundo transparente -->
                    <stroke
                        android:color="@color/primary"
                        android:width="2dp"
                        />
                </shape>
            </item>

        </layer-list>
    </item>
    <!-- Estado padrão (não selecionado) -->
    <item>
        <!-- Defina uma cor transparente ou qualquer outra coisa para o estado padrão -->
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:color="@color/primary"
                android:width="2dp"
                />
        </shape>
    </item>
</selector>

